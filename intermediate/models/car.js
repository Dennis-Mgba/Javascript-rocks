export class Car {
    constructor(id) {
        this.id = id;
    }
}

// using the keyword 'export' will export the class 'Car' thus making it available for other files
// Note: we could export anything, variable, function but here we are exporting the 'Car' class
