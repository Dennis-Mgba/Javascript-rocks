/*
================================================================================
                        condition operator
================================================================================
*/
// (condition) ? true : false;
// (a > b) ? "if true run" : "else run";
let foo = 7;
let result = (foo > 5) ? true : false;
console.log(result);

// the above code is a way of writing an if else statemnet on one line
